#include <Arduino.h>
#include "./Storage.h"
#include "./RGBLed.h"
#include "./WifiConnect.h"
// #include "./BLEConnect.h"
// #include "./FaceChanger.h"

// void handleFaceChange(char *face)
// {
//     if (wifiConnect.isWifiConnected() && wifiConnect.isSocketConnected())
//     {
//         wifiConnect.sendFace(face);
//     }

//     if (face != "X")
//         rgbLed.blink(0, 255, 0, 10, 60);
//     else
//         rgbLed.blink(255, 0, 0, 10, 60);
// }

// FaceChanger faceChanger = FaceChanger(handleFaceChange);

void setup()
{
	Serial.begin(115200);  
 
	Serial.setDebugOutput(true);

	Serial.println();
	Serial.println();
	Serial.println();
 

	for(uint8_t t = 4; t > 0; t--) {
		Serial.printf("[SETUP] BOOT WAIT %d...\n", t);
		Serial.flush();
		delay(100);
	}

    wifiConnect.start();


        webSocket.on("connect", on_connect); 
        webSocket.begin("192.168.1.85", 3000, "/socket.io/??EIO=3&transport=websocket");
        webSocket.emit("face","{\"foo\":\"XXXX\"}"); 

    // if (wifiConnect.isWifiConnected())
    // {
    //     faceChanger.start();
    // }
    // else
    // {
    //     bleConnect.start();
    // }
}

void loop()
{
    // if (wifiConnect.isWifiConnected())
    // {
    //     faceChanger.loop();
    // }
    // else
    // {
    //     bleConnect.start();
    // }
    wifiConnect.loop();
}